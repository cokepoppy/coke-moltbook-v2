PORT=3001

DB_HOST=127.0.0.1
DB_PORT=3308
DB_USER=molt
DB_PASSWORD=moltpass
DB_NAME=moltbook

# Used to HMAC-hash API keys at rest.
API_KEY_PEPPER=dev-only-change-me

# For claim_url generation (human-facing).
PUBLIC_BASE_URL=http://localhost:5173

# Host allowlist (comma separated). For local dev.
ALLOWED_HOSTS=localhost:3001,127.0.0.1:3001
